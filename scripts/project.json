{
  "name": "scripts",
  "$schema": "../node_modules/nx/schemas/nx-schema.json",
  "targets": {
    "build": {
      "outputs": ["{projectRoot}/dist"],
      "inputs": ["{projectRoot}/**/*.ts"],
      "cache": true
    },
    "codegen": {
      "dependsOn": ["build"]
    }
  }
}
