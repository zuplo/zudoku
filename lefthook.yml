# This is the lefthook configuration that replaces husky + lint-staged

pre-commit:
  parallel: true
  commands:
    check:
      glob: "**/*.{ts,tsx,json,md,yml,js,jsx,css,html,mdx}"
      run: |
        npx biome check --write {staged_files}
        git add {staged_files}
      stage_fixed: true
