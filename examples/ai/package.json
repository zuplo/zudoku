{
  "name": "ai-chat",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "zudoku dev",
    "build": "zudoku build",
    "preview": "zudoku preview"
  },
  "nx": {
    "tags": [
      "example"
    ],
    "targets": {
      "build": {
        "dependsOn": [
          "zudoku:build"
        ]
      }
    }
  },
  "dependencies": {
    "@zudoku/plugin-ai": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:",
    "zudoku": "workspace:*"
  }
}
